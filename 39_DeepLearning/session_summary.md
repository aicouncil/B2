The provided file contains notes from a meeting on August 12, 2025, that started at 18:28 GMT+05:30.

Here's a summary of the meeting:

  * **Perceptron Model Review and Manual Implementation:** The meeting began with a review of the perceptron model and its manual implementation. Sunil confirmed successfully building a perceptron model from scratch. AI Council demonstrated manual implementation, detailing the use of three input neurons (TV, radio, newspaper) connected to one output neuron, calculating output with specific weights, and explaining the feed-forward process. They also discussed error calculation, gradient evaluation, and tuning the gradient through backward propagation for multiple features.
  * **Error Optimization and Learning Rate Tuning:** AI Council explained error calculation and demonstrated how a large learning rate can increase error, while a smaller one (e.g., 10^-6) decreases it. They found 10^-5 to be optimal for faster error reduction. Increasing iterations further reduced the error, and the mean absolute error for sales prediction was approximately 1.39.
  * **Introduction to Keras for Neural Network Development:** Keras was introduced as a more efficient way to build neural networks, especially deep neural networks, compared to error-prone manual processes. A four-step process for Keras was outlined: data definition, importing algorithms (models and layers), compiling the model, and training the model. Rehan Siddiqui noted a voice issue, after which AI Council reiterated Keras's benefits in handling neurons and gradient descent automatically.
  * **Deep Neural Networks and Trainable Parameters:** The concept of a deep neural network with hidden layers was explained, contrasting it with a simpler perceptron model. Hidden layers increase trainable parameters (weights and biases), enabling the model to learn complex patterns. AI Council demonstrated adding a hidden layer with 10 neurons and explained how the number of hidden layers and neurons affects total trainable parameters, a decision based on model performance (underfitting or overfitting).
  * **Model Compilation and Training with Keras:** The third step, compiling the model, involves defining the loss function (e.g., mean square error), optimizer (e.g., SGD), and metrics (e.g., mean absolute error). For the fourth step, training, `model.fit` was demonstrated with input/output data and epochs. Adjusting the learning rate for the SGD optimizer and the number of epochs significantly reduced the mean absolute error.
  * **Overfitting and Data Splitting:** Overfitting, where a model performs well on training data but poorly on test data due to memorization, was explained. `sklearn.model_selection.train_test_split` was demonstrated for splitting data into training and test sets to evaluate overfitting. A wide difference in error values between training and test sets indicates overfitting.
  * **Activation Functions and Data Normalization:** Activation functions, specifically ReLU, were introduced for breaking linearity and preventing negative neuron outputs that slow optimization and increase errors. ReLU converts negative values to zero, reducing error magnitude and speeding up learning, especially for positive sales data. Normalizing data (e.g., with `MinMaxScaler`) was recommended to further reduce error magnitudes and accelerate optimization, particularly with many trainable parameters.
  * **Hyperparameter Tuning and Future Plans:** The number of hidden neurons was reiterated as a hyperparameter adjusted based on model performance (increased for underfitting, decreased for overfitting). Reducing hidden neurons helps control overfitting and improves model stability. The neural network building process was summarized: defining layers, compiling, and training. For the next class, AI Council plans to work with larger datasets and neural networks with thousands of trainable parameters, reviewing the current material and sharing the file.
